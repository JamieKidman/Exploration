#!/bin/python3

import os

# Easily download alot from a location based on url formats that are eailsy iterable.
# could have a list of urls but this wasnt needed for this project. would be useful if you consitently have to download multiple files from differnet locations
for i in range(0,10)
    os.system("wget """"url part 1""" + str(i) """ substitute for the url change that represents the next item to download """)